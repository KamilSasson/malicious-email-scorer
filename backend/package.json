{
  "name": "malicious-email-scorer",
  "version": "0.1.0",
  "private": true,
  "description": "MaliciousEmailScorer - rule-based email risk scoring API (Node.js)",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "debug": "node --inspect src/server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
